# KURULUM — lab-app elle deploy (doldur)

> Her adımı yaptıkça buraya **gerçek komutu** yaz. Amaç: bir başkası bu dosyayı
> okuyup sıfırdan aynısını kurabilsin.

## 0. VM bilgisi
- Dağıtım / sürüm:
- Kullanıcı:

## 1. Uygulama yerleşimi
- Kopyalama komutu:
- Konum:
- Servis kullanıcısı (login'siz):

## 2. PostgreSQL
- Kurulum:
- Veritabanı + kullanıcı + yetki:
- Parolayı nereye koydun (koda DEĞİL):

## 3. systemd unit
- Dosya yolu:
- `EnvironmentFile`:
- `enable` + `start`:
- `systemctl status` çıktısı (özet):

## 4. nginx
- Config yolu:
- proxy_pass hedefi:
- test: `curl -s http://127.0.0.1/health` →

## 5. Firewall
- Açık portlar:
- 8000 dışarıya kapalı mı: (nasıl doğruladın)

## 6. Reboot testi
- `reboot` sonrası `is-active`:
- Elle müdahale gerekti mi:

## 7. En çok zamanı ne aldı?
- (Bu cevap C1'de container'ın neyi çözdüğünü anlamanın anahtarı.)
