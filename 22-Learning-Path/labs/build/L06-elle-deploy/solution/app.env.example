# /etc/lab-app/app.env — ÖRNEK. Gerçeğini VM'de üret, git'e KOYMA.
# Mod 600, sahibi labsvc. Parolayı buradan sızdırma.
APP_HOST=127.0.0.1
APP_PORT=8000
# PostgreSQL bağlantısı — gerçek parolayı sen üret (koda gömme):
PGHOST=127.0.0.1
PGDATABASE=labdb
PGUSER=labuser
PGPASSWORD=<DB_PASSWORD>
