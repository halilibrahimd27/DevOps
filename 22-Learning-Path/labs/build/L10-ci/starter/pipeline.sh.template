#!/usr/bin/env bash
# pipeline.sh — SEN doldur. test → build → tag(SHA) → push.
# Kopyala: cp pipeline.sh.template pipeline.sh, sonra TODO'ları tamamla.
set -euo pipefail          # ilk hatada dur — "yeşil yalanı" olmasın

REGISTRY="localhost:5000"
IMAGE="lab-app"
# TODO: TAG'i commit SHA'dan üret (git rev-parse --short HEAD). :latest KULLANMA.
TAG="TODO"

echo "== 1/3 test =="
# TODO: pytest -q çalıştır

echo "== 2/3 build =="
# TODO: docker build -t "$REGISTRY/$IMAGE:$TAG" .

echo "== 3/3 push =="
# TODO: docker push "$REGISTRY/$IMAGE:$TAG"

echo "OK → $REGISTRY/$IMAGE:$TAG"
